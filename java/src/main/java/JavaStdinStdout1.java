import concepts.ScannedSolution;

final class JavaStdinStdout1 extends ScannedSolution {
    JavaStdinStdout1(final String lines) {
        super(lines);
        println(scanner.nextInt());
        println(scanner.nextInt());
        println(scanner.nextInt());
    }
}
